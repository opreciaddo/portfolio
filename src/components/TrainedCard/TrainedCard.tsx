import React, { FC } from 'react';
import './TrainedCard.scss';

interface TrainedCardProps {}

const TrainedCard: FC<TrainedCardProps> = () => (
  <div className="TrainedCard">
    TrainedCard Component
  </div>
);

export default TrainedCard;
